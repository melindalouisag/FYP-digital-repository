spring:
  datasource:
    url: jdbc:hsqldb:mem:thesisrepo;sql.syntax_pgs=true;sql.lowercase_ident=true
    driver-class-name: org.hsqldb.jdbc.JDBCDriver
    username: sa
    password:
  jpa:
    hibernate:
      ddl-auto: create-drop
    properties:
      hibernate:
        dialect: org.hibernate.dialect.HSQLDialect
  flyway:
    enabled: false
  sql:
    init:
      mode: never
  servlet:
    multipart:
      max-file-size: 15MB
      max-request-size: 16MB
  test:
    context:
      default-execution-listeners:
        - org.springframework.test.context.web.ServletTestExecutionListener
        - org.springframework.test.context.support.DirtiesContextBeforeModesTestExecutionListener
        - org.springframework.test.context.event.ApplicationEventsTestExecutionListener
        - org.springframework.boot.test.context.SpringBootDependencyInjectionTestExecutionListener
        - org.springframework.test.context.support.DependencyInjectionTestExecutionListener
        - org.springframework.test.context.support.DirtiesContextTestExecutionListener

logging:
  level:
    org.hibernate.SQL: warn

file:
  storage:
    provider: memory
  max-size-bytes: 15728640

app:
  auth:
    mode: LOCAL
  security:
    csrf:
      enabled: false
    rate-limit:
      window-seconds: 60
      login:
        max-attempts: 1000
      otp:
        max-attempts: 1000
  seed:
    enabled: true
    admin-password: test-only-admin-password
    lecturer-password: test-only-lecturer-password
    student-password: test-only-student-password
